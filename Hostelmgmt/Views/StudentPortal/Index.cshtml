@model HostelManagement.Models.Student
@{
    ViewBag.Title = "My Portal";
    Layout = "~/Views/Shared/_StudentLayout.cshtml";

    var hasModel = Model != null;
    var name = hasModel ? Model.Name : string.Empty;
    var email = hasModel ? Model.Email : string.Empty;
    var phone = hasModel && !string.IsNullOrEmpty(Model.Phone) ? Model.Phone : "Not provided";
    var roomDetails = hasModel && !string.IsNullOrEmpty(Model.RoomDetails) ? Model.RoomDetails : null;
    var joinedOn = hasModel ? Model.DateOfJoining : DateTime.MinValue;
    var isActive = hasModel && Model.IsActive;
    var room = ViewBag.Room as HostelManagement.Models.Room;
}
<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card mb-4">
            <div class="card-header bg-success text-white">
                <h3 class="mb-0"><i class="fas fa-user-circle"></i> My Details</h3>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Name:</label>
                            <p class="form-control-plaintext fs-5">@name</p>
                        </div>
                        <div class="mb-3">
                            <label class="form-label
